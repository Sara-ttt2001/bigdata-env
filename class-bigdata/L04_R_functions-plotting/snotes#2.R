# rep stands for repetition
#define a function...we store it in a variable (min 30)
#the order of arguments is important, only if you execute the function, we use positional arguments (min 56)
# L-apply: it applies a function to an element of the list
# mapping :connect the structure of the data to the shape of the plot